make sure node, npm, gulp are installed:
node -v
npm -v
gulp -v


install gulp dependency:
npm install

install lib dependency:
bower install 

run gulp:
gulp

deploy:
copy build folder to the server

